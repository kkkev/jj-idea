<?xml version="1.0" encoding="UTF-8"?>
<plugins>
  <plugin id="in.kkkev.jj-idea" url="https://github.com/kkkev/jj-idea/releases/download/v0.8.12/jj-idea-0.8.12.zip" version="0.8.12">
    <idea-version since-build="251.0" />
    <name>Jujutsu VCS Integration</name>
    <description><![CDATA[
      <p>Native IntelliJ integration for <a href="https://jj-vcs.github.io/jj/">Jujutsu (jj)</a>, a Git-compatible version control system with a fundamentally different approach to working with code.</p>

      <h3>Why Jujutsu?</h3>
      <p>Jujutsu treats your working copy as a commit—always. No staging area, no "dirty" state. Just describe what you're working on and keep coding.</p>

      <h3>Key Features</h3>
      <ul>
          <li><b>Describe-First Workflow</b> — Working Copy tool window for describing current work and creating new changes.</li>
          <li><b>Custom Log View</b> — Visual commit graph with inline change IDs, descriptions, and bookmarks.</li>
          <li><b>Change Operations</b> — Edit, abandon, and describe changes directly from the log.</li>
          <li><b>File History &amp; Annotations</b> — Full file history with diff viewer and line-by-line blame.</li>
          <li><b>Multi-Repository Support</b> — Work with multiple JJ repositories in a single project.</li>
          <li><b>Real-Time Status</b> — Auto-refresh keeps the UI in sync as you edit files.</li>
      </ul>

      <p><a href="https://github.com/kkkev/jj-idea">GitHub</a> · <a href="https://github.com/kkkev/jj-idea/issues">Issues</a></p>
    ]]></description>
    <vendor email="jj-idea@kkkev.in" url="https://github.com/kkkev/jj-idea">Kevin Thomas</vendor>
    <change-notes><![CDATA[
      <h3>0.8.12</h3>
      
<h4>Added</h4>
<li>Rebase and Describe are now available as one-click buttons on the log toolbar, alongside New and Edit, with default keyboard shortcuts — previously only reachable via the right-click menu. Their right-click menu entries now show the same shortcut. Edit also gained a default keyboard shortcut of its own. ([#78](https://github.com/kkkev/jj-idea/issues/78))</li>
<li>New "Split into New Parent…" action alongside the existing "Split into New Child…" in file right-click menus, for splitting selected files off into a new commit that precedes the current one instead of one that follows it — useful for peeling off small commits backwards from a large verified set of changes. The current commit keeps its own change ID (and the working copy, if you're splitting it, stays put); only the new, split-off commit gets a new ID. The dialog is explicit about which side is which and, when splitting the working copy, about where `@` ends up either way. ([#74](https://github.com/kkkev/jj-idea/issues/74))</li>
<li>"Show History" is now available from file right-click menus in the commit details panel, the Working Copy tool window, and compare-changes panels, matching the editor's Jujutsu submenu.</li>
<li>Pressing Enter in the log search field now searches the whole repository, not just the commits currently loaded in the log window — useful for pasting a Git hash or finding an older commit by description or author. Matching commits are added into the log, and the status bar reports how many (or that none) were found.</li>

<h4>Changed</h4>
<li>The log search field now matches anywhere in a commit's full description, not just its first line.</li>

<h4>Fixed</h4>
<li>Fixed right-clicking a change-id link (e.g. a parent reference in a merge commit's details) showing an empty "Nothing Here" menu instead of that commit's usual context menu.</li>
<li>Fixed 0.8.11 failing JetBrains Marketplace's compatibility check and never going out to users — the Working Copy toolbar's Rebase/Squash/Abandon/etc. buttons and the status-bar bookmark widget used a couple of platform APIs the Marketplace flags but our own build didn't catch.</li>
      <p>See <a href="https://github.com/kkkev/jj-idea/releases/tag/v0.8.12">full release notes</a> for details.</p>
    ]]></change-notes>
  </plugin>
</plugins>

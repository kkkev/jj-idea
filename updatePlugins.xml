<?xml version="1.0" encoding="UTF-8"?>
<plugins>
  <plugin id="in.kkkev.jj-idea" url="https://github.com/kkkev/jj-idea/releases/download/v0.8.0/jj-idea-0.8.0.zip" version="0.8.0">
    <idea-version since-build="251.0" />
    <name>Jujutsu VCS Integration</name>
    <description><![CDATA[
      <p>Native IntelliJ integration for <a href="https://jj-vcs.github.io/jj/">Jujutsu (jj)</a>, a Git-compatible version control system with a fundamentally different approach to working with code.</p>

      <h3>Why Jujutsu?</h3>
      <p>Jujutsu treats your working copy as a commit—always. No staging area, no "dirty" state. Just describe what you're working on and keep coding.</p>

      <h3>Key Features</h3>
      <ul>
          <li><b>Describe-First Workflow</b> — Working Copy tool window for describing current work and creating new changes.</li>
          <li><b>Custom Log View</b> — Visual commit graph with inline change IDs, descriptions, and bookmarks.</li>
          <li><b>Change Operations</b> — Edit, abandon, and describe changes directly from the log.</li>
          <li><b>File History &amp; Annotations</b> — Full file history with diff viewer and line-by-line blame.</li>
          <li><b>Multi-Repository Support</b> — Work with multiple JJ repositories in a single project.</li>
          <li><b>Real-Time Status</b> — Auto-refresh keeps the UI in sync as you edit files.</li>
      </ul>

      <p><a href="https://github.com/kkkev/jj-idea">GitHub</a> · <a href="https://github.com/kkkev/jj-idea/issues">Issues</a></p>
    ]]></description>
    <vendor email="jj-idea@kkkev.in" url="https://github.com/kkkev/jj-idea">Kevin Thomas</vendor>
    <change-notes><![CDATA[
      <h3>0.8.0</h3>
      
<h4>Changed</h4>
<li>Rebase and duplicate now use jj's `--onto` flag instead of the deprecated `-d`/`--destination`, ahead of its removal in a future jj release.</li>

<h4>Fixed</h4>
<li>Fixed a crash risk on IntelliJ/RustRover 2025.1 and 2025.2 (builds 251/252) when initialising a repo or when VCS roots changed.</li>
<li>The "Resolve Conflicts…" action now opens the merge dialog on IntelliJ/RustRover 2026.2 (build 262) instead of silently doing nothing. ([#55](https://github.com/kkkev/jj-idea/issues/55))</li>
<li>The Split dialog's file checkboxes now work the way you'd expect: ticking a file moves it into the new child commit (unticked files stay in the parent), nothing is ticked by default, and the child's description field is shown above the parent's, matching their order in the log. Right-clicking file(s) and choosing "Split into New Child" now starts those files ticked, so they land in the new child commit instead of the parent. ([#54](https://github.com/kkkev/jj-idea/issues/54))</li>
<li>"Pick Hunks…" now has a fixed, always-reversible layout: a "Before" reference on one side and the child's content on the other, with an editable "Parent" in the middle you can pull hunks into or out of from either side, as many times as you like, instead of a one-way pick with no way to change your mind. Picking only some of a file's hunks no longer forces the whole file to look fully assigned to the child. Reopening "Pick Hunks…" on a file you already partially split now resumes where you left off instead of resetting.</li>
<li>Splitting with individual hunks picked (via "Pick Hunks…") no longer fails with "Failed to edit diff" / `NoClassDefFoundError: kotlin/jvm/internal/Intrinsics` on some IDE installs.</li>
<li>"Annotate Previous Revision" now re-annotates at the correct parent commit instead of an arbitrary one. On a line introduced by a merge commit, where there's no single correct "previous" revision, the action is unavailable rather than guessing wrong.</li>
<li>Annotating a file whose working copy (or an ancestor) is a merge commit no longer fails with "Revset `@-` resolved to more than one revision".</li>
<li>Annotating a resolved merge conflict no longer shows a spurious "number of lines annotated is not equal to number of lines in the file" warning. Blame now correctly attributes each line to whichever original commit introduced it, with conflict-resolution lines attributed to the merge itself.</li>
<li>Annotating a merge no longer fails with "No such path" when the file exists in only some of the merge's parents (e.g. a criss-cross merge where a file was added on only one side). Blame is now assembled from whichever parents actually have the file.</li>
<li>Pressing Enter in the Working Copy tool window's description box inserts a newline again. On 2026.2, it instead opened the current commit's diff — the log's "Enter shows diff" shortcut was firing even while typing in the description field, not just when the log itself had focus. ([#57](https://github.com/kkkev/jj-idea/issues/57))</li>
<li>On 2026.2, the committer name/email and date chips in the commit details panel could render as two overlapping icons instead of one, breaking the line-wrap point between them.</li>
      <p>See <a href="https://github.com/kkkev/jj-idea/releases/tag/v0.8.0">full release notes</a> for details.</p>
    ]]></change-notes>
  </plugin>
</plugins>

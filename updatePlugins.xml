<?xml version="1.0" encoding="UTF-8"?>
<plugins>
  <plugin id="in.kkkev.jj-idea" url="https://github.com/kkkev/jj-idea/releases/download/v0.8.5/jj-idea-0.8.5.zip" version="0.8.5">
    <idea-version since-build="251.0" />
    <name>Jujutsu VCS Integration</name>
    <description><![CDATA[
      <p>Native IntelliJ integration for <a href="https://jj-vcs.github.io/jj/">Jujutsu (jj)</a>, a Git-compatible version control system with a fundamentally different approach to working with code.</p>

      <h3>Why Jujutsu?</h3>
      <p>Jujutsu treats your working copy as a commit—always. No staging area, no "dirty" state. Just describe what you're working on and keep coding.</p>

      <h3>Key Features</h3>
      <ul>
          <li><b>Describe-First Workflow</b> — Working Copy tool window for describing current work and creating new changes.</li>
          <li><b>Custom Log View</b> — Visual commit graph with inline change IDs, descriptions, and bookmarks.</li>
          <li><b>Change Operations</b> — Edit, abandon, and describe changes directly from the log.</li>
          <li><b>File History &amp; Annotations</b> — Full file history with diff viewer and line-by-line blame.</li>
          <li><b>Multi-Repository Support</b> — Work with multiple JJ repositories in a single project.</li>
          <li><b>Real-Time Status</b> — Auto-refresh keeps the UI in sync as you edit files.</li>
      </ul>

      <p><a href="https://github.com/kkkev/jj-idea">GitHub</a> · <a href="https://github.com/kkkev/jj-idea/issues">Issues</a></p>
    ]]></description>
    <vendor email="jj-idea@kkkev.in" url="https://github.com/kkkev/jj-idea">Kevin Thomas</vendor>
    <change-notes><![CDATA[
      <h3>0.8.5</h3>
      
<h4>Changed</h4>
<li>In a jj-only project, the standard IDE "Commit" tool window and its "Local Changes" tab are now hidden — they only ever showed a half-working commit dialog and a look-alike changes list that lacked jj's actions, which confused people into using it instead of the "Working copy" tool window. This also removes the "Resolve" link on the Commit tool window's conflict node, which never worked for jj conflicts. A new "Hide the standard Commit tool window" setting (Settings → Version Control → Jujutsu) lets you turn it back on; projects that also use Git alongside jj are unaffected either way. ([#56](https://github.com/kkkev/jj-idea/issues/56))</li>
<li>The "Working copy" tool window now opens automatically (without stealing focus) the first time you open a jj project, and a one-time tip explains what it's for and how it replaces the standard Commit panel. ([#56](https://github.com/kkkev/jj-idea/issues/56))</li>
<li>"Compare with Working Copy" and "Show Diff in New Tab" (for multiple files) now open a Changes pane listing every differing file, with the current one highlighted, instead of a bare diff with only a file-selector dropdown. Selecting a file in the list updates the diff in place. ([#52](https://github.com/kkkev/jj-idea/issues/52))</li>
<li>Author/committer names in the log and commit details pane are now real hyperlinks, link-colored at rest and underlining only while hovered: click a name to email them, right-click for more options including filtering by author, with a checkmark on whichever author filter is currently active (clicking the same one again toggles it off). ([#51](https://github.com/kkkev/jj-idea/issues/51))</li>
<li>Bookmark/tag chips in the log and commit details pane are right-click only — filter the log to that reference, rename/delete/etc., with a checkmark on whichever reference filter is active. Hovering one shows a subtle grey background highlight instead of a hand cursor, since there's no left-click action to imply — a prerequisite for a future release that turns issue-tracker references *inside* a bookmark/tag name into their own links, which would otherwise be confusing nested inside a whole-chip hyperlink.</li>
<li>"Resolve Conflicts…" now opens conflicted files one at a time in the three-way merge tool instead of a single dialog listing all of them together, as part of the fix below.</li>
<li>The log's References filter dropdown now shows a disabled "Loading bookmarks and tags…" placeholder instead of looking empty right after opening the project/log, while bookmarks and tags are still loading in the background.</li>

<h4>Fixed</h4>
<li>Closing the conflict resolution window without resolving (or after resolving only some conflicts) no longer discards a side of the conflict, for every "Resolve Conflicts…" entry point jj-idea provides (Working Copy panel, commit details pane, log context menu, editor/Project view). Each conflicted file is now resolved through its own merge window; cancelling leaves the file's conflict untouched. ([#63](https://github.com/kkkev/jj-idea/issues/63)) **Known gap:** the native IntelliJ Commit tool window's own "Resolve" link (on the "Merge Conflicts" node) is separate platform UI that jj-idea cannot intercept, and is still affected — see docs/manual-tests.md for details; avoid it for jj conflicts for now.</li>
<li>"Resolve Conflicts…" no longer appears on a non-conflicted file just because some other, unrelated file in the repository has a conflict.</li>
<li>Hovering a bookmark or tag in the "Switch Working Copy" popup (opened from the status bar widget) no longer shows a broken-image icon in the tooltip.</li>
<li>Bookmark/tag chip icons in tooltips (and the commit details pane) are now vertically aligned with the surrounding text instead of floating above it.</li>
      <p>See <a href="https://github.com/kkkev/jj-idea/releases/tag/v0.8.5">full release notes</a> for details.</p>
    ]]></change-notes>
  </plugin>
</plugins>

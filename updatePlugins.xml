<?xml version="1.0" encoding="UTF-8"?>
<plugins>
  <plugin id="in.kkkev.jj-idea" url="https://github.com/kkkev/jj-idea/releases/download/v0.8.9/jj-idea-0.8.9.zip" version="0.8.9">
    <idea-version since-build="251.0" />
    <name>Jujutsu VCS Integration</name>
    <description><![CDATA[
      <p>Native IntelliJ integration for <a href="https://jj-vcs.github.io/jj/">Jujutsu (jj)</a>, a Git-compatible version control system with a fundamentally different approach to working with code.</p>

      <h3>Why Jujutsu?</h3>
      <p>Jujutsu treats your working copy as a commit—always. No staging area, no "dirty" state. Just describe what you're working on and keep coding.</p>

      <h3>Key Features</h3>
      <ul>
          <li><b>Describe-First Workflow</b> — Working Copy tool window for describing current work and creating new changes.</li>
          <li><b>Custom Log View</b> — Visual commit graph with inline change IDs, descriptions, and bookmarks.</li>
          <li><b>Change Operations</b> — Edit, abandon, and describe changes directly from the log.</li>
          <li><b>File History &amp; Annotations</b> — Full file history with diff viewer and line-by-line blame.</li>
          <li><b>Multi-Repository Support</b> — Work with multiple JJ repositories in a single project.</li>
          <li><b>Real-Time Status</b> — Auto-refresh keeps the UI in sync as you edit files.</li>
      </ul>

      <p><a href="https://github.com/kkkev/jj-idea">GitHub</a> · <a href="https://github.com/kkkev/jj-idea/issues">Issues</a></p>
    ]]></description>
    <vendor email="jj-idea@kkkev.in" url="https://github.com/kkkev/jj-idea">Kevin Thomas</vendor>
    <change-notes><![CDATA[
      <h3>0.8.9</h3>
      
<h4>Added</h4>
<li>New "Advance Bookmark Here" action moves the bookmark nearest the working copy forward to it in one click (`jj bookmark advance`), instead of requiring a manual "Move Bookmark Here…"; if more than one bookmark is equally close, you're prompted to choose which to move. Available from the bookmark widget in the log toolbar and from the log's right-click menu. Also added a per-bookmark "Advance … to Working Copy" action alongside each bookmark's existing Rename/Delete/Forget actions. Requires jj 0.39 or later — on an older jj, these actions are shown disabled, with the reason (e.g. "needs jj 0.39+") shown right in the menu item's text as well as its tooltip, and a link to upgrade in Settings. ([#61](https://github.com/kkkev/jj-idea/issues/61))</li>
<li>The bookmark widget in the log toolbar no longer goes blank once you move past every bookmark (e.g. after `jj new`) — it now shows the nearest bookmark behind the working copy and how many changes behind it is, e.g. `main +3`. ([#62](https://github.com/kkkev/jj-idea/issues/62))</li>
<li>The "+N more" indicator for collapsed bookmark/tag labels in the log now shows a coloured bookmark (or tag) icon instead of plain grey text, so a change with many bookmarks is still recognisable as a branch head even when its labels overflow; it shows the tracked-bookmark icon when every hidden bookmark is tracked, and the same hover background highlight as a bookmark/tag chip. Clicking it now also shows the correct tracked/untracked/tag icon next to each hidden bookmark or tag in the popup, and fixes all its menu items showing as disabled when there were many hidden bookmarks. ([#49](https://github.com/kkkev/jj-idea/issues/49))</li>
<li>New "Compare Before with Another Commit…" action in the Jujutsu file menus, comparing a file's parent version against any bookmark, change ID, or revision you pick.</li>

<h4>Fixed</h4>
<li>"Annotate" is now available when a file is opened from a historical commit (e.g. from the log or File History) — it used to be greyed out.</li>
<li>The diff viewer's right-click menu no longer shows "Annotate" twice (once at top level, once under "Jujutsu").</li>
<li>Fixed a rare UI freeze risk: "Annotate Previous Revision" could block the UI thread while checking whether a line's parent revision still has the file.</li>
<li>In a multi-root project, filtering the log to a bookmark or tag no longer pulls in unrelated commits from your *other* repositories, and the graph no longer draws a connecting line between different repositories' commits. Filtering to "@" (working copy) now also shows every repository's working copy, not just the first one's.</li>
<li>"Annotate Previous Revision" no longer shows a raw error when clicked on a line whose change added the file (so there's no earlier version to show) — the action is simply unavailable for that line instead, matching how it already behaves for merge-commit lines.</li>
<li>On repositories where computing blame is slow, the editor no longer eagerly re-computes it for every file you open — it now backs off automatically once blame has been consistently slow, and only computes it on demand when you actually open the annotation gutter. Cached blame is also now refreshed whenever the working copy changes, instead of potentially showing stale attribution. ([#64](https://github.com/kkkev/jj-idea/issues/64))</li>
<li>Fixed a build issue that could have prevented the plugin from being verified as compatible with IntelliJ IDEA 2025.1.</li>
<li>Viewing a file's diff no longer jumps back to the top whenever a background process (e.g. an AI coding assistant) edits a file elsewhere in the repository. Viewing the working copy's own diff also now shows live content instead of a stale snapshot if the file changes while its diff is open. ([#58](https://github.com/kkkev/jj-idea/issues/58))</li>
<li>Improved plugin compatibility by removing dependency on IntelliJ internal action API.</li>
<li>Bookmark, tag, working-copy (`@`), conflict and divergent-change colours in the log and annotation gutter are now readable on a light theme background — the bookmark gold in particular was nearly invisible. ([#51](https://github.com/kkkev/jj-idea/issues/51))</li>
<li>Fixed a memory leak that kept every project you'd opened pinned in memory for the rest of the IDE session, once its repository icon had been drawn anywhere (e.g. a multi-root VCS changes tree or the bookmark widget).</li>
<li>After clicking "Initialize" on the uninitialized-repository notification (or via VCS → Jujutsu → Initialize), the Working Copy tool window now shows the repository's changes and file statuses/editor colours update immediately, instead of staying empty/stale until some unrelated later action.</li>
      <p>See <a href="https://github.com/kkkev/jj-idea/releases/tag/v0.8.9">full release notes</a> for details.</p>
    ]]></change-notes>
  </plugin>
</plugins>

<?xml version="1.0" encoding="UTF-8"?>
<plugins>
  <plugin id="in.kkkev.jj-idea" url="https://github.com/kkkev/jj-idea/releases/download/v0.8.6/jj-idea-0.8.6.zip" version="0.8.6">
    <idea-version since-build="251.0" />
    <name>Jujutsu VCS Integration</name>
    <description><![CDATA[
      <p>Native IntelliJ integration for <a href="https://jj-vcs.github.io/jj/">Jujutsu (jj)</a>, a Git-compatible version control system with a fundamentally different approach to working with code.</p>

      <h3>Why Jujutsu?</h3>
      <p>Jujutsu treats your working copy as a commit—always. No staging area, no "dirty" state. Just describe what you're working on and keep coding.</p>

      <h3>Key Features</h3>
      <ul>
          <li><b>Describe-First Workflow</b> — Working Copy tool window for describing current work and creating new changes.</li>
          <li><b>Custom Log View</b> — Visual commit graph with inline change IDs, descriptions, and bookmarks.</li>
          <li><b>Change Operations</b> — Edit, abandon, and describe changes directly from the log.</li>
          <li><b>File History &amp; Annotations</b> — Full file history with diff viewer and line-by-line blame.</li>
          <li><b>Multi-Repository Support</b> — Work with multiple JJ repositories in a single project.</li>
          <li><b>Real-Time Status</b> — Auto-refresh keeps the UI in sync as you edit files.</li>
      </ul>

      <p><a href="https://github.com/kkkev/jj-idea">GitHub</a> · <a href="https://github.com/kkkev/jj-idea/issues">Issues</a></p>
    ]]></description>
    <vendor email="jj-idea@kkkev.in" url="https://github.com/kkkev/jj-idea">Kevin Thomas</vendor>
    <change-notes><![CDATA[
      <h3>0.8.6</h3>
      
<h4>Changed</h4>
<li>Conflicted files in the "Working copy" tool window are now grouped under a "Merge Conflicts" node at the top of the tree with a one-click "Resolve" link, and the toolbar gained a "Resolve All Conflicts" button — matching the grouped resolve affordance the standard Commit tool window used to provide, before it was hidden. ([#56](https://github.com/kkkev/jj-idea/issues/56))</li>
<li>Opening a conflicted file now shows a banner at the top of the editor with a one-click "Resolve" link, so you don't need to already know about the Jujutsu submenu to find conflict resolution. ([#56](https://github.com/kkkev/jj-idea/issues/56))</li>
<li>Issue-tracker references (e.g. `JIRA-123`) in a commit's description now render as clickable links in the log table itself, not just the commit details pane — link-colored, underlining only the exact reference while hovered, with the same left-click-to-open/right-click "Open" menu as elsewhere. Requires an Issue Navigation pattern configured under Settings → Version Control. ([#15](https://github.com/kkkev/jj-idea/issues/15))</li>
<li>Issue-tracker references (e.g. `JIRA-123`) *inside* a bookmark or tag name (e.g. a bookmark named `jira-123-fix-thing`) are now their own clickable link within the chip, separate from the chip's own right-click menu — link-colored, hand cursor and underline only on that exact reference, opening the same as elsewhere on left-click, in both the log table and the commit details pane. ([#15](https://github.com/kkkev/jj-idea/issues/15))</li>
<li>The log's hover tooltip now stays open when you move the pointer into it, so you can click its links or select and copy its text, and it now disappears as soon as you scroll instead of lingering over the view. ([#51](https://github.com/kkkev/jj-idea/issues/51))</li>
<li>Bookmark and tag creation, bookmark deletion, and bookmark forgetting now show distinctive icons instead of generic ones. A bookmark pending deletion shows a dashed outline in the log instead of just strikethrough text, and a conflicted bookmark shows a distinct red icon instead of a generic warning triangle.</li>

<h4>Fixed</h4>
<li>Annotating (blaming) a file that contains a null byte anywhere in its content no longer fails with an error; a null byte in a Kotlin source file was previously enough to break annotation for that entire file.</li>
<li>Resolving a conflict where one side deleted the file (a "modify/delete" conflict) by accepting the deleted side now actually removes the file, instead of leaving behind an empty file. Failures while resolving a conflict now show an error notification instead of failing silently.</li>
<li>Annotating (blaming) a file in a very large repository no longer fails with a blank error message. Annotation now gets a longer time limit before giving up, and if it does still run out of time you'll see a clear message explaining why instead of an empty one. ([#64](https://github.com/kkkev/jj-idea/issues/64))</li>
<li>Saving a file no longer switches the editor to the diff preview tab (opened by double-clicking a file in the Working Copy panel or commit details pane). ([#67](https://github.com/kkkev/jj-idea/issues/67))</li>
<li>Fixed the Push button sometimes doing nothing: switching the remote (or, in a multi-repository project, the repository) in the push dialog before clicking Push could silently reset your bookmark selection and leave the button unresponsive. Also, pushing a bookmark that was already up to date no longer shows a generic "Push complete" message — it now shows jj's own "Nothing changed" notice.</li>
<li>In a repository containing any divergent change, "Move Bookmark Here…" and "Move '\<bookmark\>' to Change…" showed every destination as a backward/sideways move, requiring you to tick "Allow backward or sideways move" even for a genuine forward move onto a descendant.</li>
      <p>See <a href="https://github.com/kkkev/jj-idea/releases/tag/v0.8.6">full release notes</a> for details.</p>
    ]]></change-notes>
  </plugin>
</plugins>

<?xml version="1.0" encoding="UTF-8"?>
<plugins>
  <plugin id="in.kkkev.jj-idea" url="https://github.com/kkkev/jj-idea/releases/download/v0.8.14/jj-idea-0.8.14.zip" version="0.8.14">
    <idea-version since-build="251.0" />
    <name>Jujutsu VCS Integration</name>
    <description><![CDATA[
      <p>Native IntelliJ integration for <a href="https://jj-vcs.github.io/jj/">Jujutsu (jj)</a>, a Git-compatible version control system with a fundamentally different approach to working with code.</p>

      <h3>Why Jujutsu?</h3>
      <p>Jujutsu treats your working copy as a commit—always. No staging area, no "dirty" state. Just describe what you're working on and keep coding.</p>

      <h3>Key Features</h3>
      <ul>
          <li><b>Describe-First Workflow</b> — Working Copy tool window for describing current work and creating new changes.</li>
          <li><b>Custom Log View</b> — Visual commit graph with inline change IDs, descriptions, and bookmarks.</li>
          <li><b>Change Operations</b> — Edit, abandon, and describe changes directly from the log.</li>
          <li><b>File History &amp; Annotations</b> — Full file history with diff viewer and line-by-line blame.</li>
          <li><b>Multi-Repository Support</b> — Work with multiple JJ repositories in a single project.</li>
          <li><b>Real-Time Status</b> — Auto-refresh keeps the UI in sync as you edit files.</li>
      </ul>

      <p><a href="https://github.com/kkkev/jj-idea">GitHub</a> · <a href="https://github.com/kkkev/jj-idea/issues">Issues</a></p>
    ]]></description>
    <vendor email="jj-idea@kkkev.in" url="https://github.com/kkkev/jj-idea">Kevin Thomas</vendor>
    <change-notes><![CDATA[
      <h3>0.8.14</h3>
      
<h4>Added</h4>
<li>Editor gutter change markers and Annotate (blame) can now be based on the latest immutable ancestor (trunk) or any revset you choose, instead of always the working copy's parent — useful when you have several commits stacked on top of trunk and want to see everything changed so far, not just the last one. Configure it in Settings → Version Control → Jujutsu → Diff Base, project-wide or per repository; the Local Changes / Working copy view is unaffected. ([#43](https://github.com/kkkev/jj-idea/issues/43))</li>
<li>A dedicated bookmarks panel to the left of the log table, expanded by default: a tree of every repo's bookmarks and tags, colour-matched to their log-table chips, with `feature/A`/`feature/B`-style names shown as a hierarchy instead of a flat list, matching the Git plugin's Branches panel. Its "@" node carries the same working-copy marker and colour as the log, and an issue-tracker reference embedded in a bookmark or tag name (e.g. `JIRA-123-fix-thing`) is a clickable link, same as everywhere else in the plugin. Collapse/expand it via the always-visible strip at the panel's edge or the toolbar's View Options popup. Right-click a bookmark for the same actions as its dropdown menu, plus "Filter Log to Bookmark" and "Navigate Log to Bookmark". ([#48](https://github.com/kkkev/jj-idea/issues/48))</li>
<li>The log's right-click menu gained "Compare with Another Commit..." and "Compare Before with Another Commit...", letting you diff a whole commit (or its parent) against any bookmark, change ID, or revision you pick, not just the working copy.</li>
<li>Every description field in the plugin — Describe, Split, Squash Into, New Change, and the Working Copy tool window — is now a real commit-message editor instead of a plain text box: spellcheck, subject-length/format highlighting, and a recent-messages history popup (Ctrl+E / Cmd+E), shared with the IDE's own Git commit UI. IdeaVim users get proper Insert-mode support (Escape no longer closes the dialog). ([#46](https://github.com/kkkev/jj-idea/issues/46))</li>
<li>The Working Copy tool window's file list, and the log's commit details pane when showing the working copy, now work with the IDE's own file actions — Reformat Code and Optimize Imports act on the files you've selected there, the same as in the built-in Commit view. Right-clicking a file there also gained the standard Annotate action. ([#94](https://github.com/kkkev/jj-idea/pull/94))</li>
<li>A one-time, dismissible notification now tells you when upgrading your `jj` executable would unlock additional plugin features (starting with Advance Bookmark, which needs jj 0.39+), with a link straight to the settings page to update the executable path.</li>

<h4>Changed</h4>
<li>The commit pickers in Rebase, Squash Into…, Duplicate Onto…, and Move Bookmark to Change now use the same search as the log window: regex, match-case, and whole-words toggles, and bookmark-name matching. Pressing Enter with no match in the currently-loaded list runs a one-off search of the whole repository (the same fallback the log window's search already has), so you can now rebase onto, squash into, duplicate onto, or move a bookmark to a commit that isn't in the loaded window — for example by pasting its full change ID or Git hash.</li>

<h4>Fixed</h4>
<li>Hovering a commit row's tooltip in the Rebase, Squash Into…, Duplicate Onto…, and New Change dialogs no longer shows a broken-image icon in place of bookmark/tag chips.</li>
      <p>See <a href="https://github.com/kkkev/jj-idea/releases/tag/v0.8.14">full release notes</a> for details.</p>
    ]]></change-notes>
  </plugin>
</plugins>
